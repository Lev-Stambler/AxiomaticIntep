# RAVEL-only benchmark configuration
enabled_benchmarks:
  - ravel

directions_path: directions.pkl
direction_indices: null
orthonormalize_featurizer: true
output_format: json

ravel:
  entity_types:
    - cities
  attributes_per_entity: 2
  ravel_repo_path: null
  use_cached_data: true

mib:
  tasks: []
  max_samples_per_task: -1
  hf_cache_dir: null
